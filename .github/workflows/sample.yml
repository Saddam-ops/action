- name: Build and push Docker image
  on:
    push:
      branches:
        - master
        - main
  jobs:
  - name: Build
    buid:
      runs-on: ubuntu-latest
      steps:
      - name: Checkout code
        uses: actions/checkout@v2
